module.exports = {
  validation_msg:
    "Password must be at least 8 characters long, including 1 digit and 1 letter.",
  validation_pattern: "^(?=.*\\d)(?=.*[a-zA-Z])(?=.*[a-zA-Z0-9]).{8,}$",
  passive_income: 200,
};
